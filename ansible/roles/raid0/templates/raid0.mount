[Unit]
Description=Mount RAID0 at {{ raid0_mount_path }}
DefaultDependencies=no
After=raid0.service local-fs-pre.target
Before=local-fs.target

[Mount]
What=/dev/md/ephemeral
Where={{ raid0_mount_path }}
Type=xfs
Options=defaults

[Install]
WantedBy=local-fs.target
