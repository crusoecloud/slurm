- name: Mount /var/spool/slurmctld
  ansible.posix.mount:
    src: "nfs.crusoecloudcompute.com:/volumes/{{ slurmctld_disk_id }}"
    path: /var/spool/slurmctld
    opts: "proto=tcp,nconnect=16,spread_reads,spread_writes,remoteports=dns"
    state: mounted
    fstype: nfs

- name: Install override.conf
  ansible.builtin.template:
    src: override.conf
    dest: /etc/systemd/system/slurmctld.service.d/override.conf
    owner: root
    group: root
    mode: '0644'

- name: Install slurm conf
  ansible.builtin.import_role:
    name: slurm_conf

- name: Restart slurmctld
  ansible.builtin.systemd_service:
    name: slurmctld
    state: restarted
    enabled: true
    daemon_reload: true
